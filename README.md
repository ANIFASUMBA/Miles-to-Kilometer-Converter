This Java program converts distance from miles to kilometers using a simple formula.
It prompts the user to enter  distance miles via the console, reads the input using a `Scanner`, and multiplies it by 1.60934 to get kilometers
The result is then displayed with a formatted output.
This program demonstrates basic input/output and arithmetic operations in Java
